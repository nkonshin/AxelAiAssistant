# TODO — Будущие улучшения

## UI: Загрузка резюме и вакансии через интерфейс

Сейчас `profile.md` и `job_description.md` редактируются вручную в файлах. Нужно:

- Добавить в Settings панель раздел "Профиль" — загрузка резюме (PDF/текст), парсинг и сохранение в `profile.md`
- Добавить раздел "Вакансия" — перед созвоном вставить описание вакансии, сохраняется в `job_description.md`
- Бэкенд: эндпоинты `POST /settings/profile` и `POST /settings/job` для приёма и сохранения данных
- Опционально: автоматическое извлечение ключевой информации из PDF через LLM
